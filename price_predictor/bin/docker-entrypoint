#!/bin/bash -e

# If starting the web server, ensure DB exists. Production uses SQLite under
# storage/; with a fresh named volume that directory is empty, so migrations
# must run before Puma/Rails serves traffic.
if [ "${1}" = "./bin/rails" ] && [ "${2}" = "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
